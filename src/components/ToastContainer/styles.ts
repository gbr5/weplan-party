import styled from 'styled-components';
import '../../styles/global';

export const Container = styled.div`
  position: fixed;
  right: 0;
  top: 0;
  padding: 0;
  overflow: hidden;
  z-index: 100;
`;
